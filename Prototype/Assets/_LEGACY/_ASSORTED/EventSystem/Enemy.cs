using UnityEngine;

public class Enemy : MonoBehaviour
{
	/*
	void Damage(Color color)
	{
		transform.GetComponent<Renderer>().material.SetColor("_BaseColor", color);
	}

	void DebugTell(string text)
	{
		Debug.Log(text);
	}

	void OnEnable()
	{
		Player.onEnemyHit += Damage;
		Player.onEnemyText += DebugTell;
	}

	void OnDisable()
	{
		Player.onEnemyHit -= Damage;
		Player.onEnemyText -= DebugTell;
	}
	 */
}